namespace AppRegrando
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();

            //string login = Preferences.Get("UsuariosUsername", string.Empty);
            //lblLogin.Text = $"Login: {login}";
        }
    }
}
